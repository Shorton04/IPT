TABLES: viaufkst, afvc, jest, cabn, ausp, inob, cdhdr, tj02t.

SELECTION-SCREEN BEGIN OF BLOCK b1 WITH FRAME TITLE TEXT-001.

  SELECT-OPTIONS: so_werks FOR viaufkst-iwerk OBLIGATORY,
                  so_auart FOR viaufkst-auart OBLIGATORY,
                  so_aufnr FOR viaufkst-aufnr,
                  so_date  FOR sy-datum NO-EXTENSION,
                  so_time  FOR sy-uzeit NO-EXTENSION.

  PARAMETERS: p_test AS CHECKBOX DEFAULT ''.

SELECTION-SCREEN END OF BLOCK b1.

INITIALIZATION.
  so_date-low = sy-datum.
  so_date-high = sy-datum.
  so_time-low = sy-uzeit.
  so_time-high = sy-uzeit.
  so_date-sign = 'I'.
  so_date-option = 'EQ'.
  APPEND so_date.